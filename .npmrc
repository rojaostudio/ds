auto-install-peers=true

# O escopo @rojaostudio e PUBLICO no npmjs desde a 1.0.0 (#102) — nao precisa de registry
# alternativo nem de token pra instalar. As versoes 0.x seguem no GitHub Packages, congeladas,
# para quem ainda nao migrou (#103).
